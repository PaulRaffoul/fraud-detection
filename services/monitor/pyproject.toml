[project]
name = "monitor"
version = "0.1.0"
description = "Drift detection and monitoring service"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.5",
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "prometheus-client>=0.21",
]
