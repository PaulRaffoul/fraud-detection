[project]
name = "producer"
version = "0.1.0"
description = "Synthetic transaction producer with drift mode"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.5",
    "prometheus-client>=0.21",
]
